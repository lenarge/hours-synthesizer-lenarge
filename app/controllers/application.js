import Ember from 'ember';

export default Ember.Controller.extend({
  /*
  var days = params[0];
  var hours = Math.floor(days*24));
  var minutes = Math.floor(((days*24)-Math.floor(days*24))*60);

  return ("00"+hours).slice(-2) + ":" + ("00"+minutes).slice(-2);
  * */
});

import { formatHours } from 'hours-synthesizer/helpers/format-hours';
import { module, test } from 'qunit';

module('Unit | Helper | format hours');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatHours([42]);
  assert.ok(result);
});
